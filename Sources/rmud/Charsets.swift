import Foundation


/*
class Charsets {
    static let sharedInstance = Charsets()
    
    func applyTable(to data: [CChar], table: [CChar]) -> [CChar] {
        var result = [CChar]()
        result.reserveCapacity(data.count)
        for c in data {
            result.append(table[Int(UInt8(bitPattern: c))])
        }
        return result
    }
}
*/
